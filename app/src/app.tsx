import * as styles from "./index.styles";
import * as React from "react";
import { SomeComponent } from "@system";

export default () => {
  return (
    <div className={styles.square}>
      <SomeComponent />
    </div>
  );
};
