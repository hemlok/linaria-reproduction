import * as React from "react";
import * as classes from "./SomeComponent.styles";

export function SomeComponent({}) {
  return (
    <div className={classes.button}>
      <p className={classes.text}>button</p>
    </div>
  );
}
